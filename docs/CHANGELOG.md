# 更新日志

本文件记录 Autoforge 桌面应用的版本变更。脚本包规范版本（`autoforge.json` 中的 `"autoforge": "1.0"`）与应用 semver 独立维护。

---

## [1.11.0] — 2026-07-04

### 新增

- **功能弹窗** — 设置、执行历史、开发指南改为居中模态弹窗（`AppFeatureModal`），互斥打开、Esc / 遮罩关闭

### 改进

- 侧边栏分类列表独立滚动，底部「导入脚本」按钮不再被挤出可视区
- 详情面板 Tab 栏：当前 Tab 自动滚入视口、悬停显示横向滚动条、滚轮横向滚动
- 设置面板懒加载：首次打开时才初始化配置
- 脚本创建 Skill（`autoforge-script-create`）流程与 reference 速查优化

---

## [1.10.0] — 2026-07-03

### 新增

- **开发 / 生产数据隔离** — `autoforge-development/` 与 `autoforge-production/` 独立 userData；旧版扁平目录自动迁移并修复库内绝对路径
- **运行二次确认** — 详情面板与卡片快捷运行前弹出确认对话框
- **`npm run prod`** — `build` + `preview` 快捷命令

### 改进

- `electron-vite` 区分 `development` / `production` mode（`.env.development` / `.env.production`）
- 新增 `app-data-root` 服务：环境根目录解析、数据种子复制、路径重写与修复

---

## [1.9.0] — 2026-07-03

### 新增

- 脚本入口兼容 `main(ctx)` 命名（JS / Python 均支持 `run` → `main` 解析）
- 定时任务卡片显示 Cron 执行周期摘要

### 改进

- 脚本卡片与终端面板布局优化
- 滚动条与整体 UI 样式调整

---

## [1.8.0] — 2026-07-02

### 新增

- 点击执行历史条目打开详情弹窗，查看单次运行完整信息
- 开发指南支持下载 Markdown 格式脚本规范（`script-spec.md`）

### 改进

- 脚本列表分页（每页 12 条）
- 复制交互优化
- 暗色主题上下文菜单 UI 修复

---

## [1.7.0] — 2026-07-01

### 改进

- 小记模块 UI 与交互优化
- 修复无法添加多条小记条目的问题

---

## [1.6.0] — 2026-06-30

### 新增

- **Python 脚本运行时** — 子进程执行、`autoforge_runtime` 协议、pip 依赖与全局 Python 依赖
- 脚本列表按上传时间排序
- 依赖安装结果缓存，避免重复安装

### 改进

- Python 子进程 stdout UTF-8 编码修复
- 全局 Python 依赖 API 调用路径修正

---

## [1.5.2] — 2026-06-29

### 改进

- 运行结果支持选择性复制
- 保存操作新增提示反馈
- 编辑器交互与 UI 细节优化
- 面板固定逻辑

---

## [1.5.1] — 2026-06-29

### 新增

- 小记模块独立于环境 Profile，不与全局变量绑定

### 改进

- 打开脚本目录入口位置调整
- 悬浮窗鼠标悬停抓手效果去除

---

## [1.5.0] — 2026-06-28

### 新增

- **小记面板** — 快捷保存常用文本片段，一键插入到当前输入框
- 设置页显示应用版本号

### 改进

- 6 套深浅皮肤（锻炉 / 曜石 / 松野 / 暖纸 / 素纸 / 浅樱）

---

## [1.4.0] — 2026-06-27

### 改进

- 运行结果面板默认折叠
- 删除与编辑相关 bug 修复
- 功能与 UI 细节优化

---

## [1.2.0] — 2026-06-26

### 新增

- **SQLite 持久化** — `autoforge.db` 替代 JSON 文件；旧数据自动迁移
- **运行进度协议** — `ctx.stage()` / `ctx.progress()`，UI 实时展示阶段与进度条
- 环境变量与运行参数多类型 schema（文本、数字、布尔、选项等）
- 参数变量按环境 Profile 独立绑定
- 全局动态注册快捷键
- 脚本开发 Skill：`autoforge-script-create`

### 改进

- 校验逻辑与 UI 多处修复
- 开发文档更新

---

## [1.1.0] — 2026-06-25

### 改进

- 终端面板优化
- UI 问题修复

---

## [1.0.0] — 2026-06-28

首个正式版本。Autoforge 从 ScriptBox 原型演进为本机脚本锻造与管理平台，具备完整的脚本生命周期管理能力。

### 平台能力

- **脚本管理** — 导入脚本包或单文件，支持搜索、排序、收藏、归档与自定义分类
- **拖拽导入** — 将脚本目录或 `.js` / `.mjs` / `.cjs` 文件拖入主窗口即可导入
- **在线编辑** — 应用内编辑 `autoforge.json` 与入口代码
- **多环境 Profile** — 开发 / 测试 / 生产等环境变量集，脚本按环境独立配置
- **运行参数** — 文本参数与附件参数（`type: "attachment"`），支持多文件上传
- **运行与日志** — 一键运行、停止、重启；实时日志；运行结果展示
- **执行历史** — 按日汇总运行记录，支持搜索、状态筛选与 90 天自动清理
- **定时任务** — 六段 Cron 表达式（秒 分 时 日 月 周）自动触发
- **依赖管理** — 脚本级 npm 依赖 + 全局共享依赖
- **浏览器自动化** — 内置 Playwright bundle；脚本级无头模式开关
- **窗口模式** — 托盘常驻、桌面悬浮球、全局快捷键 `Ctrl+Shift+A`
- **主题** — 浅色 / 深色皮肤切换
- **示例与开发指南** — 内置示例脚本与完整脚本开发规范

### 脚本规范（autoforge 1.0）

- `env` / `params` 双维度输入模型
- 附件类型运行参数（JSON 数组，含 `name` / `path`）
- 脚本级 `browser.headless` 配置
- ScriptBox 旧格式自动迁移

### 技术栈

Electron 34 · Vue 3 · TypeScript · Tailwind CSS 4 · Playwright Core · node-cron · sql.js

### 平台要求

- Windows x64（主要目标平台）
- Node.js 18+（开发构建）
- Python 3.9+（运行 Python 脚本，v1.6.0 起）

### 已知限制

- JavaScript 脚本在主进程 Node 环境运行，暂无子进程沙箱隔离
- 环境变量中的 secret 类型尚未加密存储
- 无脚本包版本历史与回滚
- 无全局并发限制

详见 [v1.0.0 版本说明](./v1.0.0.md)、[v1.9.0 版本说明](./v1.9.0.md)、[v1.10.0 版本说明](./v1.10.0.md) 与 [v1.11.0 版本说明](./v1.11.0.md)。

---

## 历史开发阶段（1.0.0 之前）

以下为 MVP 至 RC 阶段的主要里程碑，未单独发版：

| 阶段 | 要点 |
|------|------|
| MVP | ScriptBox 原型、脚本导入与运行、环境 Profile |
| 参数维度 | 区分 `env` 与 `params` |
| 附件参数 | `type: "attachment"` 与 `script-inputs/` 缓存 |
| 无头模式 | 脚本级 `browser.headless` |
| 分类系统 | 内置 + 自定义分类管理 |
| 拖拽上传 | 主窗口拖放导入 |
| 多窗口模式 | 托盘、悬浮球、全局快捷键 |
| 执行历史 | 持久化与 UI 面板 |
